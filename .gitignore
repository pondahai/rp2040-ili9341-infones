# Copyrighted material — never commit.
# The Famicom Disk System BIOS (disksys.rom) is Nintendo code; it belongs on
# the SD card root at runtime. A local copy here is fine for convenience.
disksys.rom
*.rom

# Disk images and cartridge ROMs
*.fds
*.nes

# Build output
build/
build_*/

# 偏移模式與預設模式並存時的 build 目錄
build_offset/
build_default/
